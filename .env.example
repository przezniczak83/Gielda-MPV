# ─── Gielda-MPV — Environment Variables Example ──────────────────────────────
# Skopiuj do app/.env.local i uzupełnij wartościami z Supabase Dashboard.
# NIE commituj .env.local do repozytorium.

# ── Supabase ──────────────────────────────────────────────────────────────────

# URL projektu Supabase (publiczny, bezpieczny w przeglądarce)
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co

# Anon key — tylko do odczytu publicznego (RLS); bezpieczny w przeglądarce
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Service Role key — NIGDY nie eksponuj w przeglądarce; tylko serwer/Edge Functions
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ── Next.js API Security ──────────────────────────────────────────────────────

# Klucz do POST/DELETE /api/news (manual ingest gateway)
# Generuj: openssl rand -hex 32
INGEST_API_KEY=

# Feature flag: włącz manual ingest (Month 2+)
# MVP default: false (POST zwraca 503)
ENABLE_PUBLIC_INGEST=false

# IP allowlist dla POST/DELETE (opcjonalny; przecinkowa lista)
# Serverless = dynamiczne IP Vercel → zostawić puste w produkcji
# ALLOWED_IPS=

# ── Supabase Edge Functions (ustawiane przez CLI / Supabase Secrets) ──────────
# Te zmienne NIE trafiają do .env.local — ustawia je Supabase CLI:
#   supabase secrets set CRON_SECRET=<sekret> --project-ref <ref>
#
# CRON_SECRET=<losowy-sekret-min-32-znaki>  — autoryzacja pg_cron → Edge Function
# SUPABASE_URL          — automatycznie dostępny wewnątrz Edge Function
# SUPABASE_SERVICE_ROLE_KEY — automatycznie dostępny wewnątrz Edge Function
# SUPABASE_ANON_KEY     — automatycznie dostępny wewnątrz Edge Function
